from django.urls import path

from app.apps.{{ app_name }}.web import views

# Register your urls here

urlpatterns = [path("", views.SimpleView.as_view())]

# To register this URLS
# path("{{ app_name }}/", include("app.{{ app_name }}.web.urls"))
